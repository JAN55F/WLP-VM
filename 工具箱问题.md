# 工具箱三路审查汇总（静态只读分析，行号以当前工作区为准）

## ① Bug（已实现但实现有错）

### 高（8 条）

| #    | 工具                | 问题                                                         | 证据                                                         |
| :--- | :------------------ | :----------------------------------------------------------- | :----------------------------------------------------------- |
| 1    | **点补偿**          | 注册成 `ToolType.Scaner_Kenyence`，运行时强转扫码器类必抛 InvalidCastException，流程静默中止；且未声明任何输入输出 IO | Frm_ToolBox.cs:2466 → Job.cs:12313、13233                    |
| 2    | **手眼标定**        | 眼在手外分支读表格列错位（把“组号”当像素Row、整体右移一列），还把表格占位新行算进去 → 默认模式下标定矩阵必然错误 | EyeHandCalibTool.cs:273-278（对照正确分支 287-292）          |
| 3    | **采集图像·硬触发** | 等到触发后从不给输出图像赋值 → 输出恒为上一张/空图，却报“成功”；SDK 回调里生成的图像也是局部变量用完即弃 | AcqImageTool.cs:1693-1743（赋值仅在软触发分支 1767）、SDK_HIKVision.cs:329/395 |
| 4    | **两点中点**        | 复制/粘贴时“点1”输入未做保护被重复添加 → 粘贴出的工具一运行就抛越界，流程静默中止 | Frm_ToolBox.cs:2169（对照 2174 的“点2”有保护）               |
| 5    | **OCR**             | 重启后首跑识别抛错时 catch 只 Train 不重跑 → 输出空字符串且报“成功” | OCRTool.cs:571-590                                           |
| 6    | **预处理·填充项**   | 对图像调区域算子 `FillUp` → 添加“填充”项后运行必抛 HALCON 错误；窗体还把填充项误建成 Binary；FillUp 类是空壳 | ImageProprecessingTool.cs:70-71、Frm_ImageProprecessingTool.cs:443 |
| 7    | **彩图转RGB**       | “输入图像”误加进 **output** 列表（输入列表为空、无自动连接）→ 流程运行取不到图，必报“未指定输入图像” | Frm_ToolBox.cs:271-277                                       |
| 8    | **二维码识别**      | 同样把“输入图像”挂在 output 列表（叠加②-4 的“引擎无分发分支”） | Frm_ToolBox.cs:644-650                                       |

### 中（约 20 条）

- **Job.Run 五处空输入检查写错变量**（判 IOName 恒非空而非 sourceFrom）→ 输入未链接时抛异常被大 catch 吞掉，流程静默中止：点线距离/线线距离/线线交点/线线角度/两点中点｜Job.cs:11332/11425/11662/11762/11517
- **数据分析**：空输入分支强转 BlobAnalyseTool → InvalidCastException｜Job.cs:12419
- **扫码器_基恩士**：工具被“禁用”时强转 KenyenceScanerTool1（30 Temp 里的另一个类）→ InvalidCastException｜Job.cs:12316
- **采集图像·彩转灰**：Run 开头强制 `RGBToGray=false`，用户勾选运行时完全失效｜AcqImageTool.cs:1608
- **海康SDK**：GenImage1 传原始驱动缓冲，转换后的 BGR 缓冲全部弃用 → 彩色相机(Bayer)出图错乱｜SDK_HIKVision.cs:289/395
- **海康SDK**：回调里对 SDK 内部指针 `Marshal.FreeHGlobal(pData)` 非法释放 → 堆破坏/偶发崩溃风险｜SDK_HIKVision.cs:398
- **查找圆**：成功/未找到两处绕过本类已有的 SafeInvokeFindCircleWindow 直接写控件 → 跨线程｜FindCircleTool.cs:1370-1379
- **查找圆·ignoreNum**：点数不足时崩溃 + MetrologyModel 句柄泄漏（无 finally）+“未找到圆”误报“未知原因”｜FindCircleTool.cs:1213-1260
- **条码识别**：结果计数 Label 无 Visible/Invoke 保护 → 解码成功也可能被跨线程异常打成“失败”｜BarcodeTool.cs:224
- **阵列区域**：Run 是空壳（不计算、无条件报成功），输出区域为 null 或配置期遗留值｜ArrayRegionTool.cs:105-118
- **下相机定位**：放料平移量 `Math.Abs` 丢方向（注释自己写“此处不应该取绝对值，后期应更正”）→ 差值为负时放料位置错｜DownCamAlignTool.cs:141-143
- **上/下相机定位/点位引导**：Run 跨线程直写窗体控件 → 连续运行+窗体打开时异常被吞、工具误报失败｜UpCamAlignTool.cs:143-149、DownCamAlignTool.cs:154-160、PointAlignTool.cs:147-152
- **点偏差**：类型串比较 `"List`1[VMPro.Point]"`（工程无此类）恒假 → 点序号 pointIdx 完全失效恒取第一个点；窗体两个处理器一个减 1 一个不减｜PointOffsetTool.cs:36-53、Frm_PointOffsetTool.cs:159/167
- **存储图像**：ClearFile 用 File.Delete 删**目录**必抛异常被吞 → 按天自动清理从未生效｜SaveImageTool.cs:61-76
- **存储图像**：保存队列四重隐患——图像引用不克隆、无锁、单张失败毒丸退线程、限速 1 张/秒 → 高频采集丢图+内存无界增长｜SaveImageTool.cs:25/180/220-237
- **组合线段**：Run 直接向 Frm_ImageWindow 单例窗口 DispLine → 跨线程+多窗口画错位置｜CreateLineTool.cs:104
- **距离测量**：训练/搜索角范围口径不一致（模板匹配已修的同款问题）→ 靠近结束角一侧目标匹配不到｜MeasurementTool.cs:1603-1605 vs 1694-1718
- **脚本编辑**：每次 Run 都用 CodeDom 重新编译且不缓存 → 连续生产运行内存持续增长（程序集不可卸载）｜CodeEditTool.cs:107/141-143/378-390
- **扫码器×3**：分片数据到达时前片被覆盖丢弃→误报“未扫到”（204-209）；static 串口第二实例配置被跳过（22/81-91）；英文态失败分支三元写反报“成功”（217）｜KenyenceScanerTool.cs
- **标记点**：Job.cs:11074 `value as XY` 遇 List 源为 null → NRE 静默中止（同文件 10812 有双类型防御可抄）｜Job.cs:11074-11075

### 低（6 条）

- **线线距离**：DistanceLineToLine 异常返回 -1 仍报“成功”｜DistanceLLTool.cs:103-107/152-167
- **条码识别**：BarcodeType 未按索引取值，多码时类型显示错乱｜BarcodeTool.cs:165
- **创建ROI**：中间 HObject 不释放长跑内存增长；未绘制时输出 null 仍报成功｜CreateROITool.cs:131-147
- **数据显示**：超限分支显示位置用窗口 part 偏移、正常分支用绝对坐标 → 图像平移/缩放后超限文本错位｜LabelTool.cs:157-170
- **距离测量**：dgv.Rows.Add 无 Invoke（因引擎缺分支暂不可达，修②-3 时一并处理）｜MeasurementTool.cs:1870-1880
- **点偏差**：else 分支无空表检查 → 报“未知原因”而非“输入未链接”｜PointOffsetTool.cs:53-56

## ② 未开发 / 未开放

- **高｜英文语言模式整棵工具箱错乱**：57 个叶子中仅 11 个能加对工具、21 个会**加成别的工具**（如点偏差→查找边、循环/分支→图像相减）、25 个弹“尚未开发”——树英文标签与 AddTool 英文别名是两套各自漂移的字符串且大量乱码占位｜Frm_ToolBox.cs:2662-2777 vs 137-2633
- **高｜中文模式 9 个叶子点击即弹“此工具尚未开发！”**：OCV、尺寸标定、循环、分支、拟合线、拟合圆、算术、光源_康视达、光源_乐视（AddTool 无 case）。其中拟合线/尺寸标定已有实现类可接线（FitLineTool/CircleCalibrationTool 均为空壳），其余 7 个连类都没有｜Frm_ToolBox.cs:2679/2688/2707-2708/2716-2717/2747/2756-2757
- **高｜距离测量**：引擎无 `ToolType.Measurement` 分支 → 入流程后永不执行、单工具调试能跑（极隐蔽）；类本身是模板匹配的复制体｜Job.cs:8737-13186（无 Measurement）、MeasurementTool.cs:64-95
- **高｜二维码识别**：引擎无 `ToolType.QRCode` 分支 + 类空壳（无任何成员）+ 输入挂错列表（见①-8）｜Job.cs:572 仅图标
- **中｜旋转平台/XY平台**：全链路实现齐备（AddTool case/开窗/运行都有），但工具箱树**没有入口**，只能靠旧工程文件反序列化出现｜Frm_ToolBox.cs:1118/1163、Job.cs:7188/7238/9847/9915
- **中｜点点距离/两点中点/线线角度/组合线段**：能添加能运行，但双击流程节点**无开窗 case** → 配置窗永远打不开｜Job.cs:6233-8550 缺 4 个 case
- **中｜扫码器_基恩士**：双击无开窗 case，配置窗无人 Show（与已知裁决的 AddTool:2352 NRE 同属接线缺口）
- **中｜死代码四件套**：RegionOperationTool、BatteryFirstAlignTool、KenyenceScanerTool1/2（30 Temp/31 Temp1）参与编译但无任何入口｜VMPro.csproj:226/282/289/318
- **低｜3D 检测**：空分类占位，无任何子节点｜Frm_ToolBox.cs:2770-2771
- **低**：康视达/乐视英文说明互换（AfterSelect）；“尚未开发”弹窗硬编码中文不随语言｜Frm_ToolBox.cs:3074-3081、2637

## ③ 功能不齐全

- **中｜预处理**：4 种处理只有“二值化”真正可用——“膨胀”接了空处理器、“腐蚀”菜单根本没接事件、“重置”接到注释掉的空方法｜ImageProprecessingTool.cs:43-59、Frm_ImageProprecessingTool.cs:457-460、designer.cs:488-494
- **中｜条码识别**：搜索区域 4 个形状按钮全部弹“尚未开发”，Run 只对全图解码；查找数量/最小对比度参数从不参与运算｜Frm_BarcodeTool.cs:77-104、BarcodeTool.cs:27/31/170
- **中｜数据分析**：只判定第 1 行规则且写死“输入项1”，多行规则和每行输入项选择全部不生效｜DataAnalyseTool.cs:38-46
- **中｜对位组装**：Run 逻辑全注释，仅透传输入位置并报成功｜AlignFitTool.cs:108-170
- **中｜光源_奥普特**：Run 全注释，不控灯直接报成功｜OPTLightTool.cs:34-57
- **中｜光源控制**：控制逻辑全注释 + 硬编码查找名为“其它设备”的流程（不存在则首跑 NRE）｜OptLightControlTool.cs:42-53
- **中｜点线距离**：空壳工具——AddTool 未声明任何 IO，Run 不计算直接报成功｜Frm_ToolBox.cs:1910-1941、DistancePLTool.cs
- **中｜工具窗体“复位/帮助”按钮族**：旋转平台/图像相减/条码/二维码/上相机定位二/点位引导等点击仅弹“尚未开发”或空体；查找边/上相机定位/显示编辑/查找圆的是注释体死代码
- **中｜数据显示等 IO 英文命名**：数据显示 5 个输入项英文都叫 "OutputImage"、两点中点“中点”/距离测量“距离”英文也是 OutputImage → 英文界面无法区分输入项｜Frm_ToolBox.cs:1799-1843/2185-2187/419-421
- **中｜复制/粘贴工具**：粘贴按枚举名走 AddTool 文本匹配 → 绝大多数工具粘贴直接报“尚未开发”｜Job.cs:3639-3655
- **中｜主界面零散占位**：演示模式、输出窗“停止刷新”、用户管理“删除用户”都是弹“尚未开发”｜Frm_Main.cs:5104-5106、Frm_Output.cs:349-351、Frm_UserManager.cs:125-128
- **低｜硬编码**：默认路径 `D:\VM Pro`（Configuration.cs:30、SaveImageTool.cs:21）；一键手眼标定写死 192.168.0.1:2000｜OneKeyEyeHandCalibTool.cs:25-29
- **低｜双语半拉子**：各工具窗体内按钮/标签中文写死不随语言切换｜如 Frm_ImageProprecessingTool.designer.cs:203
- **低｜复制残留死代码**：预处理窗体从模板匹配复制来的 20+ 空/注释处理器、Frm_MeasurementTool 用 shapeMatchTool 命名保存 MatchTool 等