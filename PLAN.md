# 蓝白简约 UI 重排与资源调度计划

> 状态日期：2026-09-09。本文记录当前实现状态和验证边界；现场设备未授权验证的项目不能标记为完成。

## 目标与合理性

本轮保持现有蓝白简约视觉语言，不重做业务流程和硬件协议，重点解决入口堆叠、模块分类不符合常用桌面软件习惯、多个隐藏页面仍持续刷新、工厂布局容易被用户操作覆盖等问题。方案合理，且保留既有事件处理、工具叶节点文字和工具创建映射，降低旧项目兼容风险。

## 已完成

- [x] 品牌统一为“威乐普电子科技有限公司 / WLP VM”，当前发行版本为 `1.0.0`：主标题始终保留 `WLP VM v1.0.0`，用户自定义项目名作为次级上下文；欢迎页和关于页显示正式版本，不再显示日期式构建标识。`Start`/`CVMPro` 版本资源同步为 `1.0.0.0`/`1.0.0`，旧项目文件及路径不改写。`Start` 输出仍为兼容名称 `WLP VM.exe`；`WLPVM.ico` 提供 16/20/24/32/40/48/64/128/256 共 9 个尺寸并接入 `Start.csproj`、两份 manifest 和欢迎页/主窗体宿主图标。技术兼容名 `VM Pro.sln`/`VMPro`/`CVMPro.dll` 保留。
- [x] 将统一主题细化为浅蓝 + 暖光白：颜色、字体、圆角输入/按钮/卡片与 ToolStrip 渲染集中在 `ModernUiTheme.cs`，公共窗体打开时自动应用；窗口控制区、精确数据控件与 HALCON 画布保持清晰边界。
- [x] 重绘加载页：旧高饱和蓝底图片替换为浅蓝矢量视觉图、暖白背景和柔和进度条；内部 WLP 品牌标记/退出按钮使用抗锯齿圆角绘制，保留原进度/退出语义，视觉刷新计时器仅在可见时运行；无边框顶层 `Form` 的 `Region` 外形裁切继续列为现场边界。
- [x] 重排存储图像模块：来源、存储规则、文件管理三张卡片复用原参数控件与运行事件；补齐格式选择回写、文件名/格式白名单与安全默认目录。清空操作增加受保护路径、链接目录、待写入检查和二次确认；写图与自动清理收口为线程安全单工作线程，移除每张图固定等待，窗口截图写后释放，64 项有界队列满载时背压而不丢请求。
- [x] 重排主壳层：标题带、仅保留机器命令/工作区导航的主命令栏、项目/流程/视觉/设备/系统/帮助 6 类标准菜单、工作区内容和状态栏；退出、登录、锁定、选项保留既有事件/权限语义并收入菜单。菜单展开时同步代理源命令的启用/可用/勾选状态，“选项”继续走管理员权限检查，F5/F6 与单次/连续运行按钮共用禁用门槛。标题栏隐藏与帮助菜单重复的更多按钮，只保留最小化、最大化/还原、关闭。
- [x] 将主页、视觉、运动收敛为 3 个稳定工作区；主页和运动按首次访问延迟创建，视觉快捷栏只在视觉工作区显示；主页由历史占位文字升级为只读生产概览和常用入口。
- [x] 将工具箱归并为 7 类：图像输入与预处理、检测与识别、标定与定位、几何与 ROI、逻辑与计算、设备与通信、输出与显示；保留叶节点文字、图标和工具映射。
- [x] 将流程编辑器的多行流程页签改为固定高度的“当前流程”下拉切换区，动态同步新增、删除和重命名，释放流程树编辑高度；将工具箱搜索区压缩为常规输入高度并移除底部说明卡片，让工具树占满剩余空间。
- [x] 移除流程编辑器内部重复的“单次运行/连续运行”展示，将底部命令区由 98 px 压缩为 54 px，只保留五个流程管理命令；旧按钮对象作为历史状态兼容载体隐藏保留，全局视觉快捷栏、菜单和 F5/F6 运行入口不变。
- [x] 修复流程模块拖拽：移除前计算安全插入索引，首项/端口/空白落点同步更新流程树与 `L_toolList`；连线迁移到独立双缓冲层并关闭 TreeView 原生 HotTracking，消除鼠标移动时的擦除闪烁和拖拽绘制竞争。
- [x] 调整流程端口与连线显示：右侧端口圆点完整放在连线覆盖层左侧并以短线接入；底部增加“显示全部连线”切换，关闭时无选择不显示、选择模块只显示该模块全部输入/输出关系，启动设置中的原选项同步更名并保持持久化字段兼容。
- [x] 将项目/流程/视觉/设备/系统/帮助六类菜单嵌入启动/暂停/停止/复位右侧的同一主命令行，保留原 MenuStrip 快捷键、代理和权限链，移除独立菜单行并向视觉工作区返还 30 px 高度。
- [x] 将设置页归并为 6 类：常规、项目、方案、启动、运行、用户与安全。
- [x] 更新工厂默认 Dock 布局 `Start/Config/Resources/Layout/经典布局1.config`：流程/工具箱共用右侧全高度标签列；输出/监控只占中央图像列底部，不再延伸到流程编辑器下方；经典模板只读，用户更改转存 `dockPanel.config`，仅对可明确识别的旧演示快照做内存迁移。
- [x] 将视觉快捷栏由约 30 个平铺图标进一步收敛为 4 个高频图文按钮（单次运行/连续运行/保存项目/读取图像）与 1 个“批量运行”下拉；上一张本地图像、暂停目录图自动切换和极速模式归入“视觉”菜单，全局变量继续代理 `toolStripButton34` 以保留刷新，方案/流程/布局/设备及重复或空实现入口不再占用视觉顶栏。
- [x] “项目”菜单补齐导出方案；不保留历史顶层“删除当前方案”代理，因为旧事件缺少完整 UI 刷新/持久化保障，删除统一走“系统 → 选项 → 方案管理”的受控路径。其他迁移代理只显示统一文字，不复制旧低清位图，点击仍转发到原 ToolStrip 项以保留权限、确认和刷新语义。
- [x] 重构 `CTextBox`、`CComboBox`、`CNumeric`、`CNumericUpDown` 四类共享输入控件：统一继承 `ModernInputControl`，用抗锯齿路径绘制暖白底板和浅蓝焦点边框，移除小控件的二值 `Region` 裁切；下拉、密码可见性和数值加减符号改为代码绘制。`CTextBox` 程序赋值一次实际变化最多一次事件，`CNumeric` 支持空/负号/小数点编辑中间态并在失焦时恢复最后有效值，`CNumericUpDown` 对 50/70 px 分别采用纯文本和纵向按钮布局。
- [x] 普通按钮与内容卡片使用带透明过渡像素的 PArgb 圆角背景，避免 `Region` 硬裁造成阶梯边；已有超大业务图标使用高质量插值缩小。主命令栏和视觉快捷栏图标由 `ModernVectorIconFactory` 按目标像素尺寸重绘并关闭 ToolStrip 二次缩放。
- [x] 调整资源调度：主状态 1 s 刷新；运动 IO 仅在运动工作区有效可见、非最小化且非运行时按 300 ms 刷新；运动控件刷新线程按需启动并在隐藏时降频；设备管理、运动控制和点表编辑不再由启动流程提前创建；移除主刷新路径的同步 UI `Invoke`、全局资源锁和人为延时。
- [x] 启动期间硬件初始化/连接保留在工作线程，欢迎页、主窗体、项目 UI 重建、菜单、状态栏和板卡失败提示统一回到主 UI 线程；启动完成标志改为 `volatile`，避免等待线程读取旧值。
- [x] 区分两阶段配置：`Start/Program.cs` 从 `Config\Config.ini` 预读最早提示所需语言，`VM.Init()` 在欢迎页/主窗体构造前通过 `Configuration.Read(false)` 读取 `Config\Configuration.ini` 最终配置，`Machine.InitAll()` 不再重复读取。配置读取只更新模型字段，主窗体与欢迎页不再全局关闭 WinForms 跨线程检查；流程公共图像/叠加显示入口派发回目标图像窗体的 UI 线程。
- [x] 修复进入视觉工作区时流程树 `AfterSelect` 的存量跨线程异常：取消“后台线程等待 500 ms 后直接读写 WinForms 控件”的实现，改为每个 `Job` 持有的 WinForms UI Timer 去抖；快照 `e.Node`、以请求代次淘汰旧选择，双击时取消待执行回调，完整工具预览分支留在控件所属线程。
- [x] 新安装/新配置的 `Configuration.dataPath` 默认改为 `D:\WLP VM`；已有 `Configuration.ini`/项目中序列化的 `D:\VM Pro` 不自动迁移、不移动数据，存储图像模块保留旧保存路径切换兼容分支。
- [x] 输出日志改为有界模型队列：UI 每 100 ms 最多合并 48 条、每批只滚动一次；去除锁内 `Application.DoEvents()`，保留筛选、计数、报警历史、清空与 1000 条窗口语义；报警时间键碰撞时递增 tick，保证突发报警不丢失且计数一致。
- [x] PLC、TCP 客户端/服务端、串口和扫码器配置页改为按需创建；后台只向已存在、可见且仍绑定当前设备的页面派发状态，切换 A/B 后忽略旧目标迟到结果；TCP 共享 Socket 表、旧句柄释放与手动断开自动重连边界一并收口。
- [x] 修正可独立验证的 HALCON 图像对象列表释放/所有权处理；背景、叠加对象与图形上下文由同一窗口锁保护，进程级刷新统一采用 `GraphicRenderLock → objectStackLock` 锁序。
- [x] 增加无硬件测试：`Tests/LayoutTemplateGenerator.cs` 可复现生成 Dock XML；`Tests/UiShellSmoke.cs` 覆盖 Dock 加载、工厂布局保护、壳层几何、最小尺寸、主页/视觉切换、隐藏窗体延迟创建、运动窗体惰性线程、7 类工具、6 类设置、TCP/PLC A-B 绑定与非创建式回写、跨线程检查守卫、1050 条普通日志与 1100 条报警突发/清空，并输出 `DrawToBitmap` 预览。运动工作区主动硬件刷新留待隔离设备环境验证。
- [x] 更新 `PROJECT_GUIDE.md`、`README.md`、`PLAN.md` 和 `更新日志.txt`，同步构建方法、三份 INI 职责及验证边界。

## 本机非硬件验证（2026-09-08 最终结果）

- [x] `Controls.csproj` 使用 VS2022 Build Tools MSBuild、`Debug|AnyCPU` 完成 Rebuild：0 警告、0 错误。
- [x] `VMPro.csproj` 使用正常输出目录完成 Rebuild：961 警告、0 错误；警告为历史 SDK/未使用字段等存量项。
- [x] 完整解决方案按推荐的 `/t:Build`、`Debug|Mixed Platforms` 完成：4 警告、0 错误；4 项均为 MSIL 与既有 x86/AMD64 相机或运动 SDK 的架构提示。
- [x] `UiShellSmoke` 完成隔离编译和无硬件回归，退出码 0、通过 1088 项断言；除原有 WLP VM 品牌、菜单、输入、圆角、图标与权限状态覆盖外，新增严格跨线程检查下“连续选择两个空流程节点→首页→视觉→等待 900 ms”回归，确认只处理最新选择且视觉 Dock/快捷栏保持可用。EXE 嵌入图标与源 ICO 匹配 100%，欢迎页品牌块浅蓝覆盖 80.13%。
- [x] 点击视觉修复后的 `VMPro.csproj /t:Build` 为 539 警告、0 错误；最终解决方案 `Debug|Mixed Platforms` Build 为 4 警告、0 错误。最终 `WLP VM.exe`：31,232 bytes，SHA-256 `AE425B788A48B8746CEB6A19422EE7CF87E99DBCFCF27CA2DFD97CF2E0BC642B`；`CVMPro.dll`：8,532,480 bytes，SHA-256 `11A828BF847E4144525D169F37DC1938648DF1C811706BCD40A635E11325A27A`；`Controls.dll`：68,608 bytes，SHA-256 `B737DD2C8BD9B526FBD46C0AC8FA54DF675D41BBE1BE36BA94EE606C849922D1`。`WLPVM.ico` 9 个尺寸及 manifest 标识通过。
- [x] 默认 Dock 文件、当前无 WLP VM/VM Pro/Smoke 进程、`git diff --check` 均已复核；diff-check 仅报告仓库既有 LF→CRLF 策略提示，无空白错误。

> 直接构建 `Start.csproj /p:Platform=x86` 会因 `VMPro.csproj` 不存在该项目级平台输出映射而失败；本仓库当前受支持、可复现的启动程序构建入口是解决方案 `Debug|Mixed Platforms` 映射。

> 2026-09-07 的 Smoke 1/1、4+5 快捷栏和旧 DLL 哈希属于本轮品牌/输入/图标收尾前的历史记录，不能作为 2026-09-08 当前代码的验收结论。

## 待现场验证

- [ ] 在隔离设备/仿真环境启动完整程序，确认启动欢迎页、项目恢复、主页/视觉/运动切换及退出保存链路。
- [ ] 在 1024 × 640、常用 1080p/2K 分辨率、100%/125%/150% DPI 和多屏环境检查菜单溢出、按钮命中区、长文本、Dock 拖放与最大化边界。当前启动清单未声明全局/Per-Monitor DPI 感知；启用前必须回归历史绝对布局，不能只凭 20/30/40 px 图标预览修改清单。
- [ ] 抽查其他高频工具配置窗体（采集、模板匹配、查找圆/边、PLC/TCP），确认四类共享输入、圆角按钮/卡片主题没有遮挡历史业务图标，并逐步按存储图像模块的卡片范式重排；本轮是公共控件覆盖，不等于全部 50+ 工具均已逐页重构。
- [ ] 验收外层无边框窗体和欢迎页圆角：欢迎页内部品牌/退出控件及输入/普通按钮/卡片已移除二值 `Region` 并采用抗锯齿绘制，但顶层无边框 `Form` 仍保留 WinForms `Region` 外形裁切，边缘平滑度须在目标 Windows 版本、显卡和 DPI 下检查。
- [ ] 接入真实相机、PLC、运动卡后测量 UI 切换、运动 IO 刷新、设备断线与资源锁竞争时的最坏延迟；确认机器运行时不会发起维护型 IO 轮询。
- [ ] 验证工厂布局只读、自定义布局保存、升级后模板覆盖与旧布局兼容；用真实项目确认 Dock 内容反序列化顺序。
- [ ] 使用包含多个图像窗口的旧项目回归自定义 Dock 布局；当前无硬件烟测只覆盖一个标准图像文档，不能宣称复杂旧布局已完全兼容。
- [ ] 长时间运行并记录 UI 线程占用、句柄数、托管/非托管内存、相机重复开关和运动页反复切换后的资源曲线。

## 后续独立批次

1. **相机 SDK 资源所有权（高优先级）**：核实 `SDK_HIKVision.ImageCallBack()` 回调 `pData` 的 SDK 所有权，禁止在契约不明确时直接 `Marshal.FreeHGlobal()`；重构 `SDK_MindVision` 的跨实例静态 `frameBuffer`，补齐重复枚举、多相机与关闭释放路径。必须使用厂商文档、受控真机和崩溃转储验证，不能混入纯 UI 发布。
2. **硬件 IO 可取消与超时**：为相机、PLC、运动卡刷新建立统一取消、超时和错误退避策略，量化 `Machine.lock_resources` 的等待上限。
3. **生产安全缺陷**：独立修复启动/复位互锁、手眼标定数据列与失败状态、一键标定超时/线程边界；完成前不得把本轮 UI 通过等同于整机可投产。

## 明确边界与遗漏提醒

- 本轮没有启动完整程序，没有真实相机、PLC、运动卡、现场急停/安全门或自动流程验证。
- 无硬件 `DrawToBitmap` 只能确认构造、逻辑尺寸与绘制结果，不能证明 Windows DPI 虚拟化、多屏切换、ClearType 或外窗 `Region` 的最终屏幕合成质量。
- 可见运动页的 `Machine.UpdateIO()` 仍由 UI 计时器同步调用；虽然已增加可见性、运行状态和 300 ms 门控，慢驱动或资源锁等待仍可能冻结 UI，需在隔离设备环境量化后再设计异步快照，不能直接在无硬件条件下改变运动语义。
- `Release|x86` 仅是解决方案现有配置，不是已验证发布结论；第三方 DLL/SDK 的位数和部署许可仍需逐项确认。
- 仓库存在依赖历史输出目录或外部安装目录的引用，正常整方案验证仍使用 `/t:Build`。将全局 `OutDir` 直接传给所有项目引用的隔离 `/t:Rebuild` 会使 `HWindow_Tool` 找不到历史 HALCON 引用（此前本机复核为 127 个引用错误）；本轮最终验证应使用正常输出目录构建加隔离 Start/Smoke，结果待回填。即使该路径通过，也不能宣称清理后的完整仓库可纯净重建。
- 三份 `Config.ini`/`Configuration.ini` 的路径和职责不同，不能合并，也不应再新增同名配置。尤其不能把 `Config\Config.ini` 的早期语言预读当作最终业务配置，也不能把 `Configuration.Read()` 再移回 `Machine.InitAll()`。
